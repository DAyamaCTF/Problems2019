# -*- coding: utf-8; mode: python -*-

pid='G'

problem(
  time_limit=10.0,
  id=pid,
  title=pid + ": Split a Number",
  #wiki_name="-", # for wikify plugin
  #assignees=['tsubasa'], # for wikify plugin
  #need_custom_judge=True, # for wikify plugin
  #reference_solution='tsubasa-c',
  )

atcoder_config(
  task_id=None # None means a spare
)
